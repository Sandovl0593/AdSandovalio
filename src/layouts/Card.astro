---
interface Props {
  areaSection: String;
}

const { areaSection } = Astro.props;
---

<section
  class="flex flex-col items-center rounded-lg bg-slate-800 shadow md:flex-row"
>
  <slot />
</section>

<style define:vars={{ areaSection }}>
  section {
    grid-area: var(--areaSection);
  }
</style>
