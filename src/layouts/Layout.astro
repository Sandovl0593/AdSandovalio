---
import NavBar from "@/components/NavBar.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>

  <body class="bg-slate-950 text-white">
    <span id="top"></span>
    <NavBar />
    <div id="app" class="relative flex h-full gap-4 pt-20">
      <slot />
    </div>
  </body>
</html>

<style is:global>
  @font-face {
    font-family: "RethinkSans";
    src: url("RethinkSans.ttf");
  }

  :root {
    --height-body-def: 1700px;
  }

  #app {
    display: grid;
    grid-template-areas:
      "profile info"
      "profile image1"
      "experience techs"
      "image2 techs"
      "projects projects";
    grid-template-columns: 4fr 1fr;
    grid-template-rows: 40vh 24vh 60vh 14vh auto;
  }

  html {
    scroll-behavior: smooth;
    font-family: "RethinkSans", system-ui, sans-serif;
  }

  body {
    width: 75%;
    height: var(--height-body-def);
    margin: 0 auto;
  }

  @media (max-width: 1100px) {
    body {
      height: calc(var(--height-body-def) * 1.5);
    }

    #app {
      grid-template-areas:
        "profile"
        "info"
        "image1"
        "experience"
        "techs"
        "image2"
        "projects";
      grid-template-columns: 1fr;
      grid-template-rows: 64vh 60vh 14vh 30vh 14vh 14vh auto;
    }
  }
</style>
